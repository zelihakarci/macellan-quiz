export function useComputedAnim(width, LENGTH) {
  const MAX = (LENGTH - 2) * width;
  const MIN = -MAX;
  const WL = width * LENGTH;
  return {
    MAX,
    MIN,
    WL,
    LENGTH
  };
}
//# sourceMappingURL=useComputedAnim.js.map